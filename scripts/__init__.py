# scripts/__init__.py (place in my_music_etl_project/scripts/)

# This empty file tells Python that the 'scripts' directory should be treated as a package.
# It is necessary for absolute imports (like 'from scripts.etl_processor import ...')
# and relative imports within the 'scripts' directory to work correctly.
